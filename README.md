# MOCA 3D Landing Page

This repository contains a simple landing page for **MOCA 3D**. The page is built with plain HTML and CSS.

## Development

Open `index.html` in a browser to view the page. Styles are defined in `style.css`.
